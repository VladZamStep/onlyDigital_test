import { HeaderContainer } from "./Header.styled"

const Header: React.FC = () => {
    return (
        <HeaderContainer>
            <h1>Only.</h1>
        </HeaderContainer>
    )
}

export default Header