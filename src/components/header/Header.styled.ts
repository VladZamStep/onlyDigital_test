import styled from 'styled-components'

export const HeaderContainer = styled.div`
    padding-top: 40px;
    position: absolute;
    top: 0;
    left: 50%;
    transform: translate(-50%);

    h1 {
        font-size: 4rem;
        font-weight: 700;
    }
`